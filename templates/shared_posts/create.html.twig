{% extends 'layouts/base.html.twig' %}

{% block title %}Share a post{% endblock %}

{% block body %}
    <div class="container pt-3">
        <div class="main">
            <div class="rom">
                <div class="col-md-6">
                    <h1 class="h3 mb-4">Share "{{ post.title }}" via email</h1>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn btn-primary w-100" type="submit" formnovalidate>Share post</button>
                    {{ form_end(form) }}
                </div>

            </div>
        </div>
    </div>

{% endblock %}
