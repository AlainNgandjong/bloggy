<h2>☕ What's new ?</h2>
{% cache "total_posts_count" %}
<p>I have written {{ total_posts() }} posts so far.</p>
{% endcache %}

{% with {latest_posts: latest_posts(5)} %}
    <div>
        <h3>✨Latest Posts</h3>
        {% cache "latest_posts;" ~ latest_posts|map(p => p.id)|join(',')|sha_sum256 %}
                {{ include('posts/_list.html.twig', { posts: latest_posts(5)}) }}
        {% endcache %}
    </div>
{% endwith %}

{% cache "most_commented_posts" %}
<div>
    <h3>🚀 Most commented Posts</h3>
    {{ include('posts/_list.html.twig', { posts: most_commented_posts(5)}) }}
</div>
{% endcache %}